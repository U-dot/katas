import { expect } from "chai";
import { executeInstructions } from "./code";

describe("", () => {
    describe("", () => {
        it("", () => {
            jest.spyOn(console,"log")
            executeInstructions({
                x: 0,
                y: 0,
                direction: 'N',
                plateau: {
                    width: 0,
                    height: 0,
                }
            })

        });
    });
});
